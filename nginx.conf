server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    
    include /etc/nginx/mime.types;

    location / {
        return 301 /react-counter/;
    }

    location /react-counter/ {
        try_files $uri $uri/ /react-counter/index.html;
    }
}