apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-counter-app
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: react-counter-app
  template:
    metadata:
      labels:
        app: react-counter-app
    spec:
      containers:
        - name: react-counter-app
          image: react-counter-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
