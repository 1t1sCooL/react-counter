apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: react-counter
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /react-counter
            pathType: Prefix
            backend:
              service:
                name: react-counter
                port:
                  number: 80
